<div>
  <a href="{{item.externalUrl}}">{{item.title}}</a>
  <span>{{item.points}} points by {{item.user}} {{item.timeAgo}} </span>
  {{#link-to 'item' item}} {{item.commentsCount}} Comments{{/link-to}}
  <button {{action markAsRead}}>Mark as Read </button>
</div>
